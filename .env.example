# Database
DATABASE_URL=postgresql://yogayjain@localhost:5432/aagam

# LLM Providers (server-side only — NEVER expose to frontend)
OPENAI_API_KEY=sk-xxx
TENSORMUX_API_KEY=tmx-xxx

# Tools
SEARCH_API_KEY=xxx

# Observability (server-side only — NEVER expose to frontend or client state)
NEATLOGS_API_KEY=
